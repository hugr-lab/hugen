package main

import (
	"fmt"
	"os"
)

// main is a Phase-1 placeholder. Phase 4 task T022 implements the
// real CLI dispatch (--create-template build mode, --template server
// mode) per specs/004-analyst-toolkit/contracts/python-mcp.md.
func main() {
	fmt.Fprintln(os.Stderr, "python-mcp: not yet implemented (phase-3.5 task T022 pending)")
	os.Exit(1)
}
