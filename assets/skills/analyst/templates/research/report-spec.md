# Report spec

> Filled in by the researcher ONLY when the mission's deliverable is a
> report / document. When the goal produces data, an answer, or a file
> that is not a rendered report, write "not a report mission" under the
> first heading and leave the rest. The report-builder reads this file
> to know WHAT to render — sections, format, metrics — so it does not
> re-derive the report shape from the raw goal.

## Audience & goal

<!-- Who reads this report and what decision / understanding it serves.
     One or two lines. -->

## Sections

<!-- The ordered sections the report must contain, one per line:
     <section title> — what it shows (a table / chart / KPI / prose).
     This is the report's table of contents. -->

## Format

<!-- Output format (html / markdown / pdf) and the rendering stack:
     charts (e.g. plotly), tables (e.g. great_tables), language. Lift
     output_format / chart_lib / language from the caller inputs when
     given. -->

## Key metrics

<!-- The KPIs / aggregates the report headlines, with how each is
     computed (which table / field / aggregation). Ties back to
     data-model.md so the data wave fetches exactly these. -->

## Output path

<!-- The absolute destination path for the rendered file (lift the
     user's file_path input verbatim when they gave one). -->
