# Research

> Filled in by the researcher. The planner reads this to build the
> plan on confirmed scope. Keep it lean — the schema detail lives in
> data-model.md; this file is decisions + rationale + proposed AC.

## Scope decisions

<!-- Each ambiguity in the goal and how it resolved: pinned by caller
     input, resolved by your discovery, or answered by the user. One
     line each. -->

## Rationale

<!-- WHY the scope landed where it did — the reasoning the planner
     should not re-litigate. -->

## Alternatives considered

<!-- Interpretations / sources you ruled out, and why. "none" is a
     valid answer. -->

## Proposed acceptance criteria

<!-- Criteria you suggest for "done". The PLANNER is the authority —
     these are proposals it may keep, rewrite, or drop. One per line;
     each singularly satisfiable. Mirror these in the handoff's
     ac_proposals so they reach the planner as structured input. -->

## Recipe signal

<!-- If the goal matches a known wave-shape recipe (single-source
     fetch / cross-source analysis / report deliverable / schema
     inventory), name it so the planner can pick the smallest plan.
     Otherwise "none". -->
