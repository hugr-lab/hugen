Your research artifact files did not pass validation, so the turn is
NOT done yet. Do not re-run discovery — you already have what you
need; just FIX THE FILES in place.

{{if .Reason}}What failed:
{{.Reason}}

{{end}}Fix it now:
- Edit the file(s) under `research/` with `bash.write_file` (write the
  COMPLETE file — overwrite).
- KEEP every `##` / `###` section heading from the skeleton verbatim;
  REPLACE the remaining `<!-- guidance -->` comments with your real
  content (any language). Leave no comment or `<placeholder>` behind.
- `research/data-model.md` needs the exact table type names + fields +
  join keys; `research/research.md` needs the scope decisions +
  proposed acceptance criteria.

Then re-emit your single terminal ```research``` block. Keep your
already-confirmed findings — you are correcting the FILES, not
redoing the research.
