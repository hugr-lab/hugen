## Notepad — recommended tags

When you observe a stable, reusable fact during this turn, call
`notepad:append({content, category})` once with a one-line
`content` and one of the categories below. Append in-turn — a
tool call you forget is information lost. Skip greetings,
echoes of the user, intermediate reasoning, raw tool output,
and facts already in the snapshot.

Categories advertised by the currently loaded skills:

{{range .Tags}}{{if .Hint}}- `{{.Name}}` — {{.Hint}}
{{else}}- `{{.Name}}`
{{end}}{{end}}