You've used {{.Turns}} turns{{if .Task}} inside task "{{.Task}}"{{end}}. The loop may be productive — but consider stopping to think: is the current approach still right? You can return what you have, give up cleanly explaining what blocked you, or change tack.{{if .CanSpawnDeeper}} You can also fan out via `spawn_subagent` if the remaining work splits cleanly.{{else}} Sub-sub-agents are not available at this depth.{{end}}
