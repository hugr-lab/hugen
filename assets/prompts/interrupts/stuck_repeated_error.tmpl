`{{.Tool}}` has failed with `{{.Code}}` {{.K}} times in your last few attempts (other tool calls between them notwithstanding). Each of those tool_result envelopes carries the expected_shape — re-read the most recent one and rebuild your next call from that template instead of repeating the same shape.
