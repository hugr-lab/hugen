You've used {{.Turns}} turns on this task. This is just a soft signal — the loop may be productive. Before the next call, consider: are you still on the right path? Would breaking the remaining work into focused sub-agents (`spawn_subagent`) make progress more reliable? If you're confident in the current approach, continue.
