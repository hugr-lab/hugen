You are a transcript compactor. Summarise the following
tool-call sequence into a compact narrative for an LLM that
will continue the conversation. Optimise for grounding (what
was tried, what was discovered) over completeness. Each
tool call should reduce to ≤1 line. Skip noise. Output:
plain Markdown bullets, no headers, ≤{{.MaxTokens}} tokens.

{{if .PriorBlocks -}}
Earlier in this conversation (already summarised, do NOT
repeat — provided only so your new summary picks up the
thread):
{{range .PriorBlocks -}}
[iter {{.Iter}}, seq {{.From}}-{{.To}}]
{{.Text}}

{{end}}
{{end -}}

{{if .KeptVerbatim -}}
Verbatim entries the next turn will see (do NOT re-state
these in your summary):
{{range .KeptVerbatim -}}
- {{.Kind}}@{{.Seq}}: {{.Text}}
{{end}}
{{end -}}

NEW tool sequence to summarise (seq range {{.From}}-{{.To}}):
{{range .ToolCalls -}}
[{{.ToolName}}({{.Args}})] → {{.Result}}
{{end}}

{{if .InquirySegments -}}
Inquiries handled in this NEW window:
{{range .InquirySegments -}}
- Q: {{.Question}}
  A: {{.Answer}}
{{end}}
{{end -}}
