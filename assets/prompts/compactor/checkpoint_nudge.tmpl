Context note: the current work segment is ~{{.SegmentK}}K tokens, over the ~{{.WindowK}}K window. Call context:checkpoint(description="…") to close it before any further tool calls — tool calls are blocked until you do.{{if .Fill}} {{.Fill}}.{{if and .NearBand .HasClosed}} Context is getting full — consider context:hide(cp_id) on an earlier closed segment (its detail is summarised into a placeholder, so the takeaway survives).{{else}} Plenty of headroom — no need to hide yet; just keep checkpointing.{{end}}{{end}}
