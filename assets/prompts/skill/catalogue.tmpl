## Available skills

Load any of these via the `skill:load` tool when their domain becomes relevant. Already-loaded skills are tagged `(loaded)`. Skills tagged `(recipe catalog)` bundle tested one-shot recipes — see the recipe-catalog rule in your constitution.

{{range .Skills}}- `{{.Name}}`{{if .RecipeCatalog}} (recipe catalog){{end}}{{if .Loaded}} (loaded){{end}} — {{.Description}}
{{end}}